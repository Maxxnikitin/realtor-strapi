"use strict";

/**
 * Bootstrap function that runs before the application gets started.
 * This gives you an opportunity to set up your data model,
 * run jobs, or perform some special logic.
 */

module.exports = async () => {
  // Здесь можно добавить инициализацию при запуске приложения
  console.log("Realtor CMS initialized");
};
